# AyBorg.Communication.GraphQL

## 1. Install Strawberry Shake

```bash
dotnet tool install StrawberryShake.Tools --global
```

## 2. Initialize the client

```bash
dotnet graphql init http://localhost:42000/graphql/ -n GraphQlClient
```

## 3. Build the client

```bash
dotnet build
```
